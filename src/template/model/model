<?php

namespace plugins\model\{{schema}};

use satframework\pda\DBI;
use satframework\pda\Model;

/**
 * #Table {{table}}
 * #PrimaryKey {{primary}}
 */
class {{table}} extends Model
{

    {{variables}}

    public static function Create($data)
    {
        return DBI::Prepare('{{table}}')->Save($data);
    }

    public static function Update($where, $data)
    {
        return DBI::Prepare('{{table}}')->Update($where, $data);
    }

    public static function GetAll()
    {
        return DBI::Prepare('SELECT * FROM {{table}}')->GetData();
    }

}